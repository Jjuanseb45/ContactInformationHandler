using Microsoft.Extensions.DependencyInjection;

namespace ContactInfoHandler.Infrastructure.Transversal.Configurator
{
    public static class HttpClientBaseConfigurator
    {
        public static void ConfigurarHttpClient(this IServiceCollection service, HttpClientSettings settings)
        {
            
        }
    }
}